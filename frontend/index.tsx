// Placeholder frontend entrypoint. The production dashboard usa Streamlit (streamlit_app.py/pages/*).
// Si se decide migrar a Vite/React, usar este punto de entrada.

export const placeholder = true;
